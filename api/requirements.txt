# --- Runtime -----------------------------------------------------------------
fastapi>=0.115,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
sqlalchemy[asyncio]>=2.0.35,<2.1
asyncpg>=0.30,<1.0
greenlet>=3.1                     # required by SQLAlchemy's async bridge

# --- Local dev + tests -------------------------------------------------------
# Kept in one file so `pip install -r requirements.txt` is the only command you
# need. They add ~10MB to the Vercel bundle; split them out if you ever care.
uvicorn[standard]>=0.32,<1.0
httpx>=0.27,<1.0
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<2.0
aiosqlite>=0.20,<1.0              # tests run on in-memory SQLite, no Postgres needed
